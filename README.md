# Ex4-Accessing-Files
Program that reads a given text file, scans all words, and identifies the most frequent one along with its occurrence count. In case of ties, any top word may be returned. Assumes words have up to 100 characters and ignores punctuation.
